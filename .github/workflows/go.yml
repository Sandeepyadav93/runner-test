name: Go
on: [push]
jobs:

  build:
    name: Build
    runs-on: self-hosted
    steps:

    - name: Create a file
      run: |
        echo 'hello' > $HOME/sandeep_test
        cp $HOME/sandeep_test $HOME/sandeep_test2
